# LearnMate

Каркас учебного агента на `LangChain + LangGraph + Litestar`.

Задача проекта:

- принимать длинные конспекты, статьи или расшифровки видео;
- выделять ключевые тезисы;
- генерировать тестовые вопросы для закрепления материала;
- оставлять архитектуру достаточно простой, чтобы на ней было удобно учиться LangGraph.

## Что уже есть

- `GET /v1/health` для проверки, что сервис жив;
- `POST /v1/quiz/generate` для генерации теста по тексту;
- сервис `QuizAgent`, который уже использует `LangGraph`;
- интеграция с LLM через `OPENAI_API_KEY`, если ключ задан;
- fallback-режим без LLM, чтобы можно было развивать проект даже без доступа к модели.

## Запуск

```bash
uv run python -m src.main
```

## Пример запроса

```bash
curl -X POST http://127.0.0.1:8000/v1/quiz/generate \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Основы вероятностей",
    "source_type": "text",
    "content": "Вероятность события измеряет степень его возможного наступления...",
    "question_count": 5
  }'
```

## Дальше

- подключить загрузку файлов;
- добавить извлечение текста из видео-транскриптов;
- улучшить генерацию вопросов через структурированный output;
- добавить хранение материалов и результатов в БД.
